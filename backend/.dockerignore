# Secrets — never bake into an image
.env
../.env

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# VCS / tooling
.git/
.github/

# Runtime + docs (not needed at runtime)
logs/
*.md
